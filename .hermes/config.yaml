# =============================================================================
# Xenko WhatsApp Agent — Hermes Config (Railway)
# =============================================================================

# Gateway internal port (WhatsApp bridge uses 3000; do not set to Railway PORT)
gateway:
  host: "0.0.0.0"
  port: 3000

whatsapp:
  enabled: true
  unauthorized_dm_behavior: ignore
  bridge_port: 3000

skills:
  - name: xenko-sales
    enabled: true
  - name: client-qualifier
    enabled: true

airtable:
  enabled: true
  base_id: appPcG5glvZZ8dxVQ

model:
  provider: minimax
  name: MiniMax-M2.1
