# =============================================================================
# Xenko WhatsApp Agent — Hermes Config (Railway)
# =============================================================================
# Ports: health=8080 (Railway), bridge=3000, gateway API=8642 (internal)

gateway:
  host: "127.0.0.1"
  port: 8642

whatsapp:
  enabled: true
  unauthorized_dm_behavior: ignore

skills:
  - name: xenko-sales
    enabled: true
  - name: client-qualifier
    enabled: true

airtable:
  enabled: true
  base_id: appPcG5glvZZ8dxVQ

model:
  provider: minimax
  name: MiniMax-M2.1
